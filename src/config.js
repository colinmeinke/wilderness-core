const config = {
  defaults: {
    keyframe: {
      duration: 250,
      easing: 'easeInOutQuad'
    },
    timeline: {
      alternate: false,
      delay: 0,
      initialIterations: 0,
      iterations: 1,
      reverse: false
    }
  }
}

export default config
